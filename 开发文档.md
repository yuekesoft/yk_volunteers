# 家长志愿者排班系统开发文档

## 1. 项目概述
- 本项目基于微擎（WeEngine）框架开发，模块标识为 `yk_volunteers`，用于支撑学校家长志愿者的值日排班、通知提醒与签到管理。
- 系统提供后台管理端、家长移动端微站以及模板消息通知能力，覆盖志愿者维护、时段配置、自动排班、请假与替班、签到统计等流程。
- 代码主体位于 `site.php`、`inc/` 与 `template/` 目录，通过微擎约定的入口方法（`doWeb*`、`doMobile*`）与模板系统渲染页面。

## 2. 环境与依赖
- **基础环境**：PHP 7.1+、MySQL 5.6+、微擎框架最新稳定版。
- **微擎能力**：使用微擎内置的 `pdo_*` 数据访问、`tpl_*` 表单组件、`WeAccount::create` 发送模板消息、`message()/itoast()` 页面跳转等。
- **第三方库**：
  - `framework/library/phpexcel`：用于后台导入导出 Excel（志愿者清单、报表）。
  - `UEditor`：在系统设置页用于录入节假日通知（`tpl_ueditor`）。
- **前端**：模板沿用微擎后台的 Bootstrap/jQuery，移动端页面使用微擎内置 `common/header`/`common/footer` 模板。

## 3. 目录结构说明
| 路径 | 说明 |
| --- | --- |
| `manifest.xml` | 模块基础信息声明，配置功能入口、菜单绑定与订阅消息。 |
| `site.php` | 微站主类 `Yk_volunteersModuleSite`，实现移动端请假、替班、提醒等业务入口。 |
| `inc/web/` | 后台业务入口脚本，例如 `assignments.inc.php`（排班）、`volunteers.inc.php`（志愿者）、`sysset.inc.php`（系统设置）。 |
| `inc/mobile/` | 移动端控制器：排班查看、绑定微信、扫码签到、自动提醒等。 |
| `template/` | HTML 模板，分为后台页面与 `template/mobile/` 小程序/微站页面。 |
| `images/`、`icon.jpg`、`preview.jpg` | 微擎应用素材。 |
| `.vscode/sftp.json` | IDE 自动同步配置（非运行必需）。 |
| `processor.php` / `receiver.php` | 微擎消息处理骨架，当前未实现具体逻辑。 |

## 4. 功能模块
### 4.1 后台管理端（`inc/web` + `template/`）
- **志愿者管理**（`volunteers.inc.php`、`volunteers_list.html` 等）  
  - 支持检索、分页、增改删志愿者信息，记录偏好时段、替班能力、每周限额。  
  - 提供 Excel 导入（按模板）/导出（含统计字段），使用 PHPExcel。
- **时段模板管理**（`slot.inc.php`）  
  - 定义每周各时段的显示名称、最小/最大人数，供自动排班与列表展示使用。
- **自动排班与排班维护**（`assignments.inc.php`）  
  - 展示排班列表，按日期/志愿者筛选，分页数量可在系统设置配置。  
  - 支持新增、编辑、删除排班。  
  - 包含 `auto_assign_volunteers($start_date, $holidays)` 函数：按志愿者偏好、总排班次数、节假日配置生成一周排班，写入 `yk_volunteers_assignments` 并刷新志愿者统计。
- **请假与替班记录**（`requests.inc.php`）  
  - 汇总替班历史，展示原排班人与替班人信息。  
  - `waitlist.inc.php` 预留候补管理模板，当前页面内容由 `waitlist.html` 控制。
- **报表中心**（`report.inc.php`）  
  - 提供两类统计：签到明细、家长签到次数汇总，可按日期区间筛选。  
  - 支持 Excel 导出，自动计算签到率。  
  - 可查看自动提醒日志（读取 `data/logs/cron_notice_*.php`）。
- **系统设置**（`sysset.inc.php`、`sysset.html`）  
  - 数据维护：清空志愿者/排班数据、重置累计次数。  
  - 节假日列表、节假日通知内容。  
  - 自动排班分页数量、管理员模板消息配置 (`tmplmsg_id`、`admin_openid`)。  
  - 家长自动提醒开关、提前天数、模板消息编号 (`auto_notice_tmplmsg_id`)。  
  - 时段名称映射 (`slot_labels`) 与扫码签到有效时间段 (`slot_times`)。  
  - 所有设置以 `key`/`value` 形式写入 `yk_volunteers_settings`。

### 4.2 移动端微站（`inc/mobile` + `template/mobile`）
- **排班查看**（`assignments_list.inc.php`、`assignments_list.html`）  
  - 默认展示本周排班，已绑定家长仅显示关联记录。  
  - 页面顶部可显示 `holiday_notice` 内容。
- **微信绑定**（`bind_wechat.inc.php`、`bind_wechat.html`）  
  - 家长通过姓名 + 班级匹配后绑定 `openid`。
- **请假/撤销请假**（`site.php` → `doMobileLeave` / `doMobileCancelLeave`）  
  - 更新排班状态为 `leave`/`scheduled`，可选通知管理员。
- **替班大厅 & 替班操作**（`doMobileAvailableSubs`、`doMobileReplace`、`available_subs.html`）  
  - 列出 `status='leave'` 的空缺排班，已绑定志愿者可抢替。  
  - 替班时校验重复排班、防止并发，记录到 `yk_volunteers_replacements`。
- **扫码签到**（`self_check.inc.php`、`sign.html`）  
  - 根据系统设置的 `slot_times` 判断有效时段，验证当天排班并写入签到时间/状态。
- **帮助文档**（`help.inc.php`、`help.html`）  
  - 展示排班规则、问答等静态内容。
- **自动提醒任务**（`cron_notice.inc.php`）  
  - 根据系统配置判断是否启用，取即将值班的家长并发送模板消息。  
  - 可通过定时脚本访问 `?i=UNIACID&c=entry&do=cron_notice&m=yk_volunteers` 触发。

### 4.3 Ajax/辅助入口
- `inc/web/ajax_imagefield.inc.php`：返回微擎图片上传控件 HTML，解决 iframe 内嵌渲染问题。
- `inc/web/ajax_ueditor.inc.php`：在开启 `open_basedir` 的环境下动态输出 UEditor。  
- `inc/web/ajax_sign_toggle.inc.php`：遗留自 `yk_wxsign` 模块的签到切换脚本，如无需要可后续移除或改造。

## 5. 数据库设计（核心表）
> 表前缀在代码中有两种写法：`tablename('yk_volunteers_xxx')`（自动带 `ims_` 前缀）或写死 `ims_yk_volunteers_xxx`。请确保实际建表时与微擎实例前缀一致。

### 5.1 `yk_volunteers_volunteers`（志愿者）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int PK | 主键 |
| `uniacid` | int | 公众号/小程序标识 |
| `uid` | int | 创建人员微擎 UID |
| `openid` | varchar | 微信 openid，绑定后用于通知、签到校验 |
| `name` | varchar | 家长姓名 |
| `phone` | varchar | 联系电话 |
| `child_class` | varchar | 孩子班级 |
| `prefer_slots` | text | 偏好时段列表（逗号或 JSON） |
| `can_substitute` | tinyint | 是否可替班 |
| `max_per_week` | tinyint | 每周最多排班次数 |
| `max_substitute_per_week` | tinyint | 每周最多替班次数 |
| `total_assigned` | int | 累计排班次数 |
| `last_assigned` | date | 最近排班日期 |
| `create_time` / `update_time` | datetime | 创建与最后更新 |

### 5.2 `yk_volunteers_slot_templates`（时段模板）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int PK | 主键 |
| `uniacid` | int | 公众号标识 |
| `uid` | int | 操作用户 |
| `weekday` | tinyint | 星期（1-7，当前逻辑使用 1-5） |
| `slot_code` | varchar | 时段编码（`morning`/`afternoon`/`evening` 等） |
| `display_name` | varchar | 页面展示名称 |
| `required_min` / `required_max` | tinyint | 最小/最大需求人数 |
| `create_time` | datetime | 记录创建时间（视数据表设计而定） |

### 5.3 `yk_volunteers_assignments`（排班）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int PK | 主键 |
| `uniacid` | int | 公众号标识 |
| `uid` | int | 操作人员 UID |
| `date` | date | 排班日期 |
| `weekday` | tinyint | 星期 |
| `slot_code` | varchar | 时段编码 |
| `volunteer_id` | int | 关联志愿者 |
| `role` | varchar | 角色（默认 `primary`） |
| `status` | varchar | `scheduled` / `leave` / `replaced` / `completed` / `cancel` |
| `leave_reason` | text | 请假原因 |
| `replaced_by` | int | 被谁替班 |
| `checked_in` | tinyint | 签到标记（0/1） |
| `checkin_time` | datetime/int | 签到时间，注意部分代码写入字符串 |
| `create_time` / `update_time` | datetime | 创建/更新时间 |

### 5.4 `yk_volunteers_replacements`（替班记录）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int PK | 主键 |
| `schedule_id` | int | 对应排班 ID |
| `from_volunteer` | int | 原排班人 ID |
| `to_volunteer` | int | 替班人 ID |
| `reason` | varchar | 请假原因快照 |
| `create_time` | datetime | 替班时间 |

### 5.5 `yk_volunteers_settings`（系统设置）
| 字段 | 类别 | 说明 |
| --- | --- | --- |
| `id` | int PK | 主键 |
| `uniacid` | int | 公众号标识 |
| `key` | varchar | 设置项键名（如 `holidays`、`slot_labels`、`tmplmsg_id` 等） |
| `value` | text | JSON 或字符串值 |
| `create_time` | int/datetime | 最近保存时间 |

## 6. 核心业务流程
### 6.1 自动排班（`inc/web/assignments.inc.php:196` 起）
1. 读取志愿者列表，按 `total_assigned` 升序排序。  
2. 获取时段模板与节假日配置，逐时段计算目标日期。  
3. 若目标日期命中节假日或已存在排班则跳过。  
4. 按志愿者偏好（`prefer_slots`）匹配候选，不足时补入可替班人员。  
5. 截取 `required_max` 条记录写入 `yk_volunteers_assignments`，并增量更新志愿者的 `total_assigned` 与 `last_assigned`。

### 6.2 请假与撤销
1. 家长在移动端请求请假（`doMobileLeave`），校验排班状态与志愿者归属。  
2. 更新排班状态为 `leave`，记录原因；可选发送管理员模板消息。  
3. 撤销时恢复为 `scheduled`，清空请假原因，同样可发送通知。

### 6.3 替班抢单
1. 待替班列表依据 `status='leave'` 展示。  
2. 家长替班前校验：已绑定微信、目标排班未被替、本人未在同日同段排班。  
3. 使用 `pdo_update(..., ['id'=>$id,'status'=>'leave'])` 保证并发安全，将 `volunteer_id` 切换为替班人，状态改 `replaced`。  
4. 写入 `yk_volunteers_replacements` 记录，可扩展发送模板消息。

### 6.4 扫码签到
1. 家长通过二维码进入 `self_check`，读取 `slot_times` 判断当前有效时段。  
2. 校验当天同一时段是否存在排班；若已签到提示已完成。  
3. 写入 `checked_in=1` 与 `checkin_time`，状态设为 `completed`，用于报表统计。

### 6.5 自动提醒
1. `cron_notice.inc.php` 读取 `enable_auto_notice`、`notice_time`、`auto_notice_tmplmsg_id` 等配置。  
2. 根据提前天数取目标日期的排班，遍历发送模板消息给志愿者 openid。  
3. 运行结果通过 `logging_run` 写入日志，后台报表页可查看。

## 7. 系统配置要点
- **节假日**：配置在系统设置页面，以逗号分隔保存为 JSON 数组，在自动排班与移动端提示中使用。  
- **模板消息**：  
  - `tmplmsg_id` + `admin_openid` 用于管理员请假通知。  
  - `auto_notice_tmplmsg_id` + `enable_auto_notice` + `notice_time` 用于家长提前提醒。  
- **时段名称/签到时间**：`slot_labels` 映射模板中显示名称；`slot_times` 控制扫码签到的可用时间窗。  
- **分页与展示参数**：`auto_assign_page_size` 控制自动排班列表单页条数。  
- **数据维护**：清空操作为直接 `pdo_delete`，不可恢复，需限制权限或加确认弹窗（前端已提供 `alert/confirm`）。

## 8. 开发与部署建议
- **初始化数据库**：当前 `install.sql` 为空，首次部署需手工建表（可参考上文字段设计），或补全安装脚本。  
- **本地调试**：推荐在本地微擎环境启用开发模式，通过 `?c=site&a=entry&m=yk_volunteers&do=...` 访问对应入口。  
- **代码组织**：新增后台页面时，在 `inc/web` 添加入口并创建同名模板；移动端同理。  
- **日志排查**：自定义日志位于微擎 `data/logs/` 目录，命名 `cron_notice_*.php`、`leave_notice.log` 等。  
- **模板消息调试**：确保公众号已开通模板消息能力，`admin_openid` 支持多人（逗号或换行分隔）。  
- **兼容性**：`ajax_ueditor`、`ajax_imagefield` 用于兼容受限主机环境，如无需可按需裁剪。  
- **安全性**：表单与 Ajax 入口依赖微擎权限体系，若对外开放建议增加 `checkauth()` 或自定义权限校验。

## 9. 二次开发方向
- 完成 `install.sql`，实现模块安装时自动建表。  
- 在后台排班列表补充批量导入/导出、Excel 模板。  
- 对接消息通知失败的兜底策略（如企业微信群机器人、短信）。  
- 扩展候补管理与自动调度策略（引入每周上限、替班优先级等）。  
- 整理 `processor.php`/`receiver.php`，实现公众号关键词响应或事件通知。  
- 如无需求，可移除与 `yk_wxsign` 相关的遗留文件，避免维护成本。

---
本开发文档随代码迭代更新，若有新增模块或数据表，请同步维护相关章节，确保团队成员快速了解业务脉络与技术细节。
